"use client";
import { useRouter } from "next/navigation";
import { useEffect, useState } from "react";
import { getMerchant } from "@/lib/merchants";

export default function A7Page() { const router = useRouter(); const [id, setId] = useState("giftshop"); useEffect(() => { setId(new URLSearchParams(window.location.search).get("id") ?? "giftshop"); }, []); const merchant = getMerchant(id); return <main className="detail-store-page"><header className="store-header"><button onClick={() => router.back()}>‹</button><h1>매장 상세</h1><span>♡　↗</span></header><div className="store-hero" style={{ background: `linear-gradient(135deg, ${merchant.color}, #394154)` }}><span>{merchant.category}</span></div><section className="store-intro"><span className="registered-badge">일반 등록매장</span><h2>{merchant.name}</h2><p>{merchant.description}</p><p>⌖ 부산역 도보 {merchant.walkingTime}　·　{merchant.hours}</p></section><section className="store-section"><h3>대표 상품</h3><div className="product-grid"><Product name="부산 감성 엽서 세트" price="8,000원"/><Product name="부산 캐릭터 키링" price="12,000원"/><Product name="부산 로컬 선물 세트" price="25,000원"/></div></section><section className="shipping-guide rail-guide"><h3>구매한 상품, 철도로 보내 보세요</h3><h4>KTX특송</h4><p>급하게 보내야 하는 물품을 KTX를 이용해 빠르게 운송할 수 있어요.<br/><small>역에서 접수하고 도착역에서 수령하는 빠른 배송</small></p><h4>레일택배</h4><p>급하지 않은 소형 물품은 역에서 간편하게 접수해 집까지 보낼 수 있어요.<br/><small>저렴하게 이용할 수 있는 일반 배송</small></p></section><button className="primary-store-cta secondary" onClick={() => alert("실제 서비스에서는 기존 KORAIL+ 길찾기 기능과 연결됩니다.")}>매장 가기</button></main>; }
function Product({ name, price }: { name: string; price: string }) { return <article className="product-card"><div className="product-art">◈</div><strong>{name}</strong><span>{price}</span></article>; }
