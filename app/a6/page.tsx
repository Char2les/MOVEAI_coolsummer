"use client";
import { useRouter } from "next/navigation";
import { useEffect, useState } from "react";
import { getMerchant } from "@/lib/merchants";

export default function A6Page() { const router = useRouter(); const [id, setId] = useState("samjin"); useEffect(() => { setId(new URLSearchParams(window.location.search).get("id") ?? "samjin"); }, []); const merchant = getMerchant(id); return <main className="detail-store-page"><header className="store-header"><button onClick={() => router.back()}>‹</button><h1>매장 상세</h1><span>♡　↗</span></header><div className="store-hero" style={{ background: `linear-gradient(135deg, ${merchant.color}, #26354f)` }}><span>{merchant.category}</span></div><section className="store-intro"><span className="partner-badge">제휴 매장</span><h2>{merchant.name}</h2><p>{merchant.description}</p><p>⌖ 부산역 도보 {merchant.walkingTime}　·　{merchant.hours}</p><div><span className="tag blue">철도 배송 가능</span><span className="tag green">앱 주문 가능</span></div></section><section className="store-section"><h3>대표 상품</h3><div className="product-grid"><Product name="어묵 선물 세트 1호" price="20,000원"/><Product name="프리미엄 어묵 세트" price="29,000원"/><Product name="삼진 어묵 세트" price="15,000원"/></div></section><section className="guide-section"><h3>이렇게 이용해 보세요</h3><p><b>매장에서 직접 구매</b><br/>상품을 구매한 뒤 철도배송을 이용할 수 있어요.</p><p><b>앱에서 바로 주문</b><br/>제휴 매장은 코레일 앱에서 상품을 주문할 수 있어요.</p></section><section className="shipping-guide"><h3>철도 배송 안내</h3><b>철도 배송 가능</b><p>배송 방법과 예상 운임은 주문 과정에서 확인할 수 있습니다.</p></section><button className="primary-store-cta" onClick={() => router.push("/a4")}>앱에서 주문하기</button></main>; }
function Product({ name, price }: { name: string; price: string }) { return <article className="product-card"><div className="product-art">◈</div><strong>{name}</strong><span>{price}</span></article>; }
